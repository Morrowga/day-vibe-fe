import { Box } from '@mui/material';
import ProductEssential from '@/components/ProductEssential';

const Badges = () => {
    return (
        <Box>
           <ProductEssential category='Badge' />
        </Box>
    );
};

export default Badges;
